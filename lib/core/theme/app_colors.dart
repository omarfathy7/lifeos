import 'package:flutter/material.dart';

class AppColors {
  
  static const Color cyan = Color(0xFF00F5D4);

  // ── Dark Mode Colors ──
  static const Color base = Color(0xFF0D0D0F);
  static const Color surface = Color(0xFF141418);
  static const Color textPrimary = Color(0xFFE8E8E8);
  static const Color textSecondary = Color(0xFF888888);

  // ── Light Mode Colors ──
  static const Color baseLight = Color(0xFFF8F9FA);  
  static const Color surfaceLight = Color(0xFFFFFFFF);  
  static const Color textPrimaryLight = Color(0xFF1A1D1F);  
  static const Color textSecondaryLight = Color(0xFF6F767E);  
}
